rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Secara default, hanya izinkan baca/tulis jika user terautentikasi (Anonymous login lewat PIN)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}
